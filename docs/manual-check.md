# 手動確認チェックリスト

Phase 3（`@supabase/ssr` 一括移行）と Phase 5（権限別コンポーネント統合）の検証に使う。ユニットテストでは捕まらない失敗モード、特に **RLS がサイレントに 0 行を返す**（エラーなし・画面は空のまま描画される）を、権限クラスごとに確認する。

## 前提

- 確認用に、各ロールの Google アカウント（`@future-tech-association.org`）でログインできること
- データが空だと「RLS で見えない」と「本当に 0 件」を区別できない。**確認前に次が揃っていること**
  - `/matters` を確認する **public / teamleader / accounting / admin の各アカウント自身**が、1 件以上の案件を持っていること（このページは自分の案件だけを取る。他人の案件では埋まらない）
  - `public` の案件に下書きと経理申請中が 1 件ずつあると、ライフサイクル確認に便利
  - 同じチームの別ユーザーの案件（teamleader の `/team` 用）
  - 別チームの案件（teamleader の `/team` には見えてはいけない）
  - `/team` を確認する teamleader / admin は `profiles.team` が設定済みであること。未設定だとアプリがチーム案件を取らず空一覧を返す（RLS とは別原因）
  - 経理追加収支・定期費用マスタの行（accounting / admin 用）
- ブラウザの開発者ツール Network で、対象 API が 200 かつ配列が空、になっていないかを補助的に見る。画面上は「スピナー後に空」でもエラーは出ない

## 記号

- **表示**: ページが開き、想定どおりデータ行が見える
- **空でない**: 前提データを置いたうえで、一覧・表が 0 件のままにならない
- **拒否**: `/` へリダイレクト、または未ログインなら `/login`
- **対象外**: ナビに出ない / ロール的に開けない

## 権限クラス × ページ

| ページ             | public                                     | teamleader                                             | accounting                                           | admin                                                  | RLS / データの確認観点                                                                                          |
| ------------------ | ------------------------------------------ | ------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| `/login`           | 未ログインで表示。ログイン済みなら `/`     | 同左                                                   | 同左                                                 | 同左                                                   | 認証画面。DB は読まない                                                                                         |
| `/`                | 表示。ナビは「案件カード」「新規作成」のみ | 表示。加えてチーム案件・損益計算書                     | 表示。加えて経理用一覧・損益・定期費用・経理追加収支 | 全項目                                                 | ハブのリンク先がロールと一致すること。データ件数は問わない                                                      |
| `/matters`         | **表示かつ空でない**。自分の案件だけ       | 同左（自分の案件。チーム全員分ではない）               | 同左（自分の案件。全案件ではない）                   | 同左                                                   | 他人の案件が混ざっていたら RLS またはクエリの不具合。0 件なら自分の案件が無いのか RLS かを切り分ける            |
| `/new`             | 表示。作成できる                           | 同左                                                   | 同左                                                 | 同左                                                   | 作成後 `/matters` に自分の案件が増えること                                                                      |
| `/team`            | **拒否**（`/`）                            | **表示かつ空でない**。自チームの案件（自分以外を含む） | **拒否**                                             | **表示かつ空でない**。自チーム（`profiles.team` 必須） | teamleader に別チーム案件が出たら失敗。accounting が開けたら失敗。admin のチーム未設定は空でも RLS 失敗ではない |
| `/accounting`      | **拒否**                                   | **拒否**                                               | **表示かつ空でない**。全案件                         | **表示かつ空でない**。全案件                           | 他チーム・他担当の案件が見えること。0 件は ssr 混在やセッション喪失の典型                                       |
| `/profit-loss`     | **拒否**                                   | **表示かつ空でない**（自チーム範囲）                   | **表示かつ空でない**（全体）                         | **表示かつ空でない**（全体）                           | 月を選んで売上一覧が数字として出ること。0 円ばかりで内訳が空なら RLS を疑う                                     |
| `/recurring-costs` | **拒否**                                   | **拒否**                                               | **表示かつ空でない**                                 | **表示かつ空でない**                                   | マスタ行が見えること。保存して再読込しても残ること                                                              |
| `/extra-entries`   | **拒否**                                   | **拒否**                                               | **表示かつ空でない**                                 | **表示かつ空でない**                                   | 経理追加収支の行が見えること                                                                                    |
| `/dashboard`       | **拒否**                                   | **拒否**                                               | **拒否**                                             | **表示かつ空でない**。ユーザー一覧・マスタ             | プロフィール行が 0 件なら profiles RLS またはセッション不備                                                     |
| `/auth-error`      | 未ログインでも表示                         | 同左                                                   | 同左                                                 | 同左                                                   | 認証往復が発生しないこと（middleware の open パス）                                                             |

未ログインで保護ページ（`/`, `/matters`, `/new`, `/team`, `/accounting`, `/profit-loss`, `/recurring-costs`, `/extra-entries`, `/dashboard`）を開くと `/login` へ飛ぶこと。

## 案件詳細モーダル（Phase 5 で統合する箇所）

同一案件を、許可されたロールで開いたときに **案件名・金額・取引先・コストが空欄にならない** こと。ssr 移行後にモーダルだけ 0 件になる事故をここで拾う。

| 操作                                       | public         | teamleader       | accounting        | admin |
| ------------------------------------------ | -------------- | ---------------- | ----------------- | ----- |
| `/matters` から自分の案件を開く            | 表示（編集可） | 同左             | 同左              | 同左  |
| `/team` から他メンバーの自チーム案件を開く | 対象外         | 表示（参照中心） | 対象外            | 表示  |
| `/accounting` から任意案件を開く           | 対象外         | 対象外           | 表示（経理用 UI） | 表示  |

## Phase 3（ssr 移行）で特に見る点

1. ログインできる（Cookie 形式変更の切り分け）
2. 上表で「空でない」と書いたページが、移行前と同じ件数に見える
3. 経理ページでフィルタ → テーブル / カード / モバイル幅でも絞り込みが残る（#37 PR 1-a）
4. Auth 障害時に全員が `/login` へ飛ばされず、一時的なら 503 になること（再現できなければスキップ可）

## Phase 5（権限別 UI 統合）で特に見る点

1. public / teamleader / accounting で、統合前と同じ項目が同じ権限で編集・参照できる
2. accounting 専用の確認完了・Slack 通知が public に出ない
3. 読み取り専用モーダルを開いた teamleader で、保存ボタンから更新できてしまわない

### Phase 5-a（`CostBlock` 統合）

同一コスト行について、統合前と同等の表示・操作ができること。`CostBlockForAccounting` は削除済みで、呼び出しは `variant` のみで切り替わる。

| 操作                                                    | public / teamleader（`variant="user"`）                                                                                            | accounting（`variant="accounting"`）                                                                                                         |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `/new` および `/matters` の詳細でコストを入力・削除する | コスト名 / 品目 / 支払い先 / 金額 / 支払い期限 / 通知方法 / 源泉徴収ありが編集できる。削除ボタンがある。支払い完了チェックは出ない | 対象外（経理モーダルは閲覧 + 支払い完了）                                                                                                    |
| 申請済み案件の既存コスト行                              | 入力と削除が無効。新規追加行だけ編集できる                                                                                         | 対象外                                                                                                                                       |
| `/accounting` の詳細でコストを見る                      | 対象外                                                                                                                             | コスト名 / 品目 / 価格 / 支払い期限 / 支払い先 / 申請方法 / 源泉徴収（あり・なし）が閲覧表示。支払い完了チェックがある。入力欄と削除は出ない |
| 経理確認完了後の支払い完了                              | 対象外                                                                                                                             | 支払い完了チェックが無効になる                                                                                                               |

### Phase 5-b（`BusinessBlock` 統合）

同一取引先行について、統合前と同等の表示・操作ができること。`BusinessBlockForAccounting` は削除済みで、呼び出しは `variant` のみで切り替わる。

| 操作                                                    | public / teamleader（`variant="user"`）                                                       | accounting（`variant="accounting"`）                                                          |
| ------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `/new` および `/matters` の詳細で取引先を入力・削除する | 取引先名 / 報酬額 / 請求日 / 振込期限が編集できる。削除ボタンがある。確認完了チェックは出ない | 対象外（経理モーダルは閲覧 + 確認完了）                                                       |
| 申請済み案件の既存取引先行                              | 入力と削除が無効。新規追加行だけ編集できる                                                    | 対象外                                                                                        |
| `/accounting` の詳細で取引先を見る                      | 対象外                                                                                        | 取引先名 / 請求額 / 請求日 / 振込期限が閲覧表示。確認完了チェックがある。入力欄と削除は出ない |
| 経理確認完了後の確認完了                                | 対象外                                                                                        | 受取済みチェックが無効になる                                                                  |

### Phase 5-c（`MatterCard` 統合）

同一案件カードについて、統合前と同等の表示・操作ができること。`MatterCardForAccounting` は削除済みで、呼び出しは `variant` のみで切り替わる。

| 操作                       | public / teamleader（`variant="user"`）                                                                                                               | accounting（`variant="accounting"`）                                                                                                                               |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `/matters` のカード表示    | 「開く」ボタンと三点メニュー（コピー/削除）。ステータスは下書き / 経理申請中 / 経理確認完了。担当者・未チェックコスト数・更新あり・カード選択は出ない | 対象外                                                                                                                                                             |
| `/accounting` のカード表示 | 対象外                                                                                                                                                | 「詳細」ボタン。ステータスは申請者編集中 / 経理確認待ち / 経理確認完了。担当者・未チェックコスト数が出る。`has_updates` なら更新あり。未完了カードはクリックで選択 |

### Phase 5-d（一覧 3 種の統合）

`MatterCardList` / `AccountingMatterList` / `team/TeamMatterList` は削除済み。呼び出しは `MatterList` の `variant` のみ。表示切替の viewport 判定は `useListDisplayMode` に集約（チーム一覧は従来どおりモバイルでもカード強制しない）。

| 操作           | public / teamleader（`variant="user"`） | accounting（`variant="accounting"`）                                   | teamleader / admin（`variant="readonly"`）       |
| -------------- | --------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------ |
| 一覧の表示切替 | カード（開く・三点メニュー）とテーブル  | カード（詳細・選択）とフィルタ付きテーブル。確認完了・担当者連絡がある | カード（閲覧のみ）と HTML テーブル。空なら案内文 |
| モバイル幅     | カード強制（従来どおり）                | カード強制（従来どおり）                                               | カード強制しない（従来どおり）                   |
| フィルタ       | なし                                    | 4-c のサーバ側フィルタ。ヘッダ unique 値は未フィルタ snapshot          | なし                                             |

### Phase 5-e（案件詳細モーダル 3 種の統合）

`MatterCardDetailModal` / `MatterCardDetailModalForAccounting` / `MatterCardDetailModalReadOnly` は削除済み。呼び出しは `MatterCardDetail` の `variant` のみ。閲覧専用は `useEffect` + `Promise.all` ではなく `useMatterDetail` でコスト・取引先を取る（失敗時は空配列にフォールバックしない）。

| 操作                         | public / teamleader（`variant="user"`）                                  | accounting（`variant="accounting"`）                                       | teamleader / admin（`variant="readonly"`）                       |
| ---------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `/matters` の詳細            | 更新・経理申請・取引先追加・コスト追加。下書きなら削除。経理メモは出ない | 対象外                                                                     | 対象外                                                           |
| `/accounting` の詳細         | 対象外                                                                   | 経理メモ・保存。申請中なら確認完了・下書きに戻す。追加・更新ボタンは出ない | 対象外                                                           |
| `/team` および損益の案件表示 | 対象外                                                                   | 対象外                                                                     | 閲覧専用バナーと収支サマリー。保存・更新・経理申請ボタンは出ない |

### Phase 6-a（通知基盤 + `alert` 置換）

ブラウザ標準の `alert()` は使わない。失敗・完了は画面右下の Mantine 通知。`confirm()` は 6-b まで残る。

| 操作                             | 確認観点                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------ |
| 案件の作成 / 更新 / 削除         | 成功時は緑の通知。バリデーション失敗や保存失敗は赤の通知。`alert` ダイアログは出ない |
| `/accounting` の保存・完了       | 失敗時は赤の通知。確認ダイアログ（`confirm`）は従来どおり                            |
| 経理追加収支 / 定期費用 / マスタ | 未入力は赤の通知。保存成功は緑の通知                                                 |
| ログイン（ドメイン外）           | 許可ドメイン以外は赤の通知                                                           |

## 実施記録

実施日・実施者・環境（ローカル / preview）・結果（OK / NG と画面）を、Phase 3 / Phase 5 の PR 説明に転記する。
